% !TEX root = main.tex
% ============================================================
%  Final Year Project Report Template
%  University of Newcastle — B.E. Electrical / Computer Systems
%  Based on "Preparing a Final Report" and "Report Format" guides
% ============================================================

\documentclass[11pt, a4paper, oneside]{report}

% ── Packages ─────────────────────────────────────────────────
\usepackage[
  left=32mm, right=27mm,
  top=32mm, bottom=32mm
]{geometry}

\usepackage{setspace}
\usepackage{microtype}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{graphicx}
% Chapter files sit in chapters/, so resolve figures relative to the
% report root. \includegraphics{images/foo.png} works from any of them.
\graphicspath{{./}{images/}}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{array}
\usepackage{url}
\usepackage{fancyhdr}
\usepackage{emptypage}
\usepackage{appendix}
\usepackage{pdfpages}
\usepackage{tocloft}
\usepackage{etoolbox}
\usepackage{cite}
\usepackage[hidelinks]{hyperref}  % hyperref LAST (or near last)

% ── Global Formatting ────────────────────────────────────────
\setstretch{1.35}           % 1.25–1.5 line spacing as specified

% Figure placement: bottom of page first, then top of next page
\setcounter{topnumber}{2}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{4}
\renewcommand{\topfraction}{0.85}
\renewcommand{\bottomfraction}{0.85}
\renewcommand{\textfraction}{0.15}
\renewcommand{\floatpagefraction}{0.7}

% Caption style: chapter-based numbering, e.g. Figure 2.5
\captionsetup[figure]{labelfont=bf, font=small}
\captionsetup[table]{labelfont=bf, font=small}

% Number figures/tables by chapter: Figure 1.1, Table 3.2, etc.
\counterwithin{figure}{chapter}
\counterwithin{table}{chapter}

% Equation numbering by chapter: (2.3)
\numberwithin{equation}{chapter}

% ── Hyperref Setup ───────────────────────────────────────────
\hypersetup{
  pdftitle={FYP Report Title},
  pdfauthor={Your Name},
  bookmarksnumbered=true,
  colorlinks=false,
}

% ── Page Style ───────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\fancyhead[R]{\small\leftmark}     % Chapter name on outer side
\fancyfoot[R]{\thepage}            % Page number bottom right
\renewcommand{\headrulewidth}{0.4pt}
\setlength{\headheight}{14pt}

% Plain style for chapter-opening pages
\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[R]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% ── Metadata — edit these ────────────────────────────────────
\newcommand{\reportTitle}{Household Battery Trading Optimisation in the Australian National Electricity Market}
\newcommand{\reportAuthor}{Jeremy Burns}
\newcommand{\reportYear}{2026}
\newcommand{\supervisorName}{Lawrence Ong, Jacob Madden}
\newcommand{\reportDegree}{Bachelor of Electrical and Electronic Engineering (Honours) / Bachelor of Computer Systems Engineering (Honours)}


% ============================================================
%  DOCUMENT BEGIN
% ============================================================
